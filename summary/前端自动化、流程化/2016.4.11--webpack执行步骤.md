1、学会了jquery 的 each 循环
	遍历前面的选项，内置函数中的2个参数代表序号和所循环的节点
2、字符串 split 方法复习

3、使用webpack总结

webpack执行步骤：

1开启服务；

2进入entry，默认找到index.js，作为入口文件；

3预加载（如代码检测查）；

4加载（编译scss，插入css，编译es6，图片转64，引入一系列文件）；

5开始渲染界面，生成html文件

现在已经可以使用的有：
scss,
jquery等第三方库，
热加载，
构建生成项目，
生成不同的ｊｓ文件，
使用不同的模板界面生成多页面，
为ｊｓ添加哈西。

待完善：使用模板html文件

注意：使用webpack，这是一门技术，需要很依赖webpack

