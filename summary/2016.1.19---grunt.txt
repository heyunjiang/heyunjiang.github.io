1、学习如何使用grunt自动化构建工具
	作用：自动解决反复重复的任务，简化工作，减轻劳动。
2、grunt使用步骤
	安装node.js;
	安装grunt-cli，全局目录;
	安装Grunt到项目目录下；
	建立package.json和Gruntfile.js;

	在package.json下严格定义项目构建格式；
	npm install Grunt --save -dev下载插件；
	在Gruntfile.js中配置插件信息，加载插件，设定执行顺序。
3、grunt实现了项目的自动化构建，它使用插件来实现功能，本身没有直接功能，
   插件可以实现文件压缩，代码错误检查等。。