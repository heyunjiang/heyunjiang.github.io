1、虽然jquery废气了live,但是改成了事件委托，可以实现动态绑定事件，格式：
$('#region-orgAdd').on('hide.bs.modal', function (e) {});

2、bootstrap 提供的模态框的隐藏与显示，提供了事件监听

3、在js 中写vue 的模板指针符号是不可被编译的

4、$.each 循环

5、$.extend 添加扩展，可以将对象合并到尾部

6、array.push 将东西添加到数组尾部

7、$.off()取消事件监听

8、$("#region-orgAdd-form").serialize() 可以自动将表单json序列化

心态总结：

不要着急，虽然花了时间，但是学到了东西；

多多理解同伴，现在的是配合，不要因为一点问题就生气，免得因小失大，友情最重要；

不懂的直接问，虽然现在还不能那么直接；